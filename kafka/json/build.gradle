plugins {
    id 'java-library'
}

apply from: "${rootProject.rootDir}/gradle/tests.gradle"
apply from: "${rootProject.rootDir}/gradle/publishing.gradle"

group = 'com.inomera.telco.commons'
version = project.file('VERSION').text.trim()
sourceCompatibility = JavaVersion.VERSION_17
targetCompatibility = JavaVersion.VERSION_17

configurations {
    compileOnly {
        extendsFrom annotationProcessor
    }
    testImplementation {
        extendsFrom compileOnly
    }
}
compileJava.dependsOn(processResources)

dependencies {
    api 'com.inomera.telco.commons:lang:1.0.0'
    api 'org.apache.kafka:kafka-clients:1.1.0'
    api 'org.apache.commons:commons-lang3:3.9'
    api 'com.fasterxml.jackson.core:jackson-databind:2.11.4'
    api 'com.fasterxml.jackson.datatype:jackson-datatype-jdk8:2.11.2'
    api 'com.fasterxml.jackson.datatype:jackson-datatype-jsr310:2.11.2'
    api 'com.fasterxml.jackson.module:jackson-module-parameter-names:2.11.4'

    compileOnly 'org.projectlombok:lombok:1.18.28'
    annotationProcessor 'org.projectlombok:lombok:1.18.28'

    testImplementation('org.junit.jupiter:junit-jupiter-api:5.2.0')
    testImplementation('org.junit.jupiter:junit-jupiter-params:5.2.0')
    testRuntimeOnly('org.junit.jupiter:junit-jupiter-engine:5.2.0')
    testImplementation('org.mockito:mockito-core:2.23.4')
    testImplementation('org.mockito:mockito-junit-jupiter:2.18.3')
    testImplementation('org.hamcrest:hamcrest-all:1.3')
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            groupId = "${project.group}"
            artifactId = "${project.name}"
            version = "${project.version}"

            from components.java
            artifact tasks.sourceJar
        }
    }
}
